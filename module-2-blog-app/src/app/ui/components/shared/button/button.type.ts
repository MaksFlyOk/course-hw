export enum ButtonColor {
  Green = 'green',
  Red = 'red',
}

export enum ButtonVariant {
  Filled = 'filled',
  Outlined = 'outlined',
}

export enum ButtonType {
  Button = 'button',
  Submit = 'submit',
  Reset = 'reset',
}
